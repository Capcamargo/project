# GiftMatch MVP

Веб-версия сервиса персонализированного подбора подарков.

Публичный MVP: https://giftmatch-qqdu.onrender.com

## Что делает GiftMatch

GiftMatch помогает быстро подобрать подарок по поводу, бюджету и интересам получателя. Пользователь проходит короткий сценарий, получает объяснимые рекомендации, сохраняет подходящие варианты в кабинете и при достижении лимита бесплатного плана видит тарифный шаг.

## Финальная версия сайта

- `app.html` — главная страница, каталог, форма подбора, результаты и сохранение;
- `app.css` — основные стили финальной версии;
- `app.js` — логика главной страницы и сценария подбора;
- `register.html` — регистрация и вход;
- `verify-step.html` — подтверждение входа;
- `cabinet.html` — личный кабинет пользователя;
- `admin.html` — минимальная учебная админ-панель с агрегированными показателями MVP;
- `pricing.html` — выбор тарифа;
- `payment-success.html` — учебный тестовый сценарий активации тарифа;
- `supabase/` — схема, миграции, seed-данные, RLS и запросы.

## Черновики и ранние версии

Эти файлы оставлены как следы процесса разработки и сравнения версий:

- `exam.html`;
- `exam.css`;
- `exam.js`;
- `exam-success.html`;
- `index.html`;
- `style.css`;
- `script.js`;
- `success.html`.

## Проектные документы

- `open-local.md` — как открыть проект локально;
- `demo-scenario.md` — базовый порядок показа;
- `demo-exam-scenario.md` — финальный сценарий демонстрации на экзамене;
- `deploy.md` — описание публикации на Render;
- `analytics.md` — план аналитики MVP;
- `security-checklist.md` — базовая проверка безопасности;
- `test-plan.md` — план ручного тестирования;
- `presentation-outline.md` — основа для короткой презентации;
- `mobile-mvp-plan.md` — план мобильной версии;
- `user-flow.md` — пользовательский сценарий;
- `screens.md` — экранная структура;
- `components-map.md` — карта компонентов;
- `release-plan.md` — этапы развития.

## Supabase

В проекте используется Supabase:

- `profiles` — профили пользователей, тариф, статус оплаты и роль;
- `gift_presets` — публичные пресеты подарков для каталога;
- `gift_requests` — пользовательские запросы на подбор;
- `gift_recommendations` — результаты рекомендаций и сохраненные подборки;
- Edge Functions — рекомендации, аккаунт и сохранение подборок;
- RLS — разделение пользовательских данных;
- `role = 'admin'` — учебный доступ к `admin.html`.

Подробности находятся в `supabase/README.md`, `supabase/schema.sql`, `supabase/queries.md` и `supabase/rls-notes.md`.

## Что уже умеет сайт

1. Показывать ценность сервиса на первом экране.
2. Показывать каталог готовых идей подарков.
3. Создавать пользовательский профиль через Supabase Auth.
4. Принимать параметры подбора подарка.
5. Формировать подборку с пояснениями.
6. Сохранять подборки в истории пользователя.
7. Ограничивать число сохранений на бесплатном плане.
8. Показывать paywall после достижения лимита.
9. Проводить учебный тестовый тарифный сценарий.
10. Показывать обновленный тариф в кабинете.
11. Показывать минимальную admin-сводку для профиля с ролью `admin`.

## Как открыть admin-панель

1. В Supabase SQL Editor назначить тестовому профилю роль администратора:

```sql
update public.profiles
set role = 'admin'
where email = 'admin@example.com';
```

2. Войти на сайт под этим профилем.
3. Открыть `admin.html`.

Обычный пользователь без `role = 'admin'` увидит сообщение об ограничении доступа.

## Важное ограничение MVP

Платежный сценарий является учебной имитацией. Реальная интеграция с ЮKassa, CloudPayments или другим платежным провайдером не подключена. Для production-версии потребуется server-side webhook и проверка оплаты на стороне платежного провайдера.

## Что открывать

Для локального просмотра и дальнейшей доработки использовать `app.html`. Для публичной демонстрации использовать Render-ссылку.
