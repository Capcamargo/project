{
  "name": "giftmatch",
  "private": true,
  "version": "1.0.0",
  "description": "GiftMatch static site deployment package",
  "scripts": {
    "start": "serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
